{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npx playwright test --headed",
    "headless": "npx playwright test",
    "report": "npx playwright show-report",
    "codegen": "npx playwright codegen",
    "setup": "npm i && npx playwright install && npm i --save-dev @types/node"
  },
  "keywords": [],
  "author": "Anem Shanmuka Chandra Teja",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.42.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12"
  }
}
